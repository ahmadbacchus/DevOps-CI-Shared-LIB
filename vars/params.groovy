//All Shared library Parameters needs to be declared with a starting prefix as "SL".
//the SL intify tells the pipeline that this variable is loaded by the shared library params file
SL_myCheckoutDir = "appRepo"